#include <stdio.h>
#pragma warning(disable:4996)
/*
void main() {
	int A, B;
	scanf(" %d %d", &A, &B);
	if (A > B) {
		printf(">");
	}
	if (A == B) {
		printf("=");
	}
	if (A < B) {
		printf("<");
	}
} */
