#include <stdio.h>
/*
void main() {
	int A, B, C, price = 0;
	scanf_s("%d %d %d", &A, &B, &C);
	price = A / (C - B); //C * num(num은 물건 판매량) = A + (B * num)을 정리함
	if (B >= C) { //만약 가변 비용이 물건 가격보다 높거나 같다면 = 물건값을 잘못 설정한 경우
		printf("-1");
	}
	else {
		printf("%d", price+1); //최초로 이익이 발생하는 판매량 출력
	}
}*/